NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
RESEND_API_KEY=
# Used to build links inside notification emails. Falls back to
# http://localhost:3000 if unset, so this only matters in production.
NEXT_PUBLIC_SITE_URL=
